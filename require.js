require(['a.js'], function() {

});
define([
    'require',
    'a'
], function(require, factory) {
    'use strict';

});

function require(arr, fn) {
    let arr = [];
}

function define(arr) {
    return arr;
}